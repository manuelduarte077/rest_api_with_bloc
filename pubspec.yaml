name: flutter_api_with_bloc
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  equatable: ^2.0.3
  flutter_bloc: ^7.0.1
  url_launcher: ^6.0.20


  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito:
  build_runner:

flutter:

  uses-material-design: true
